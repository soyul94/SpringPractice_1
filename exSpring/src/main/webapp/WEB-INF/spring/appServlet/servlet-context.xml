<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<!-- 어노테이션을 이용하여  Spring 웹 애플리케이션을 구현할 때 필요한 객체들을 자동 등록 -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />
	<!-- 스프링이 파일을 찾을 수 있도록 -->

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<!-- 핸들러(컨트롤러)가 뷰 이름을 반환한 경우, 뷰 이름의 앞에 "/WEB-INF/views/"를 붙이고, 뒤에 ".jsp를 붙여서 그 경로의 파일로 이동(forward)하도록 설정" -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	
	<!-- com.exam.exspring패키지 아래에서 @Component,@Controller,@Service,@Repository 등의 어노테이션이 
		 적용된 클래스들의 객체를 생성하여 스프링(객체컨테이너)에 자동 등록 -->
	<context:component-scan base-package="com.exam.exSpring" />
	
	<interceptors>
		<interceptor>
			<!-- 인터셉터를 실행할 경로에 인터셉터를 매핑함 -->
			<!-- <mapping path="/**"/> :  모든 경로에서 실행-->
			<mapping path="/**"/>
			<!-- 인터셉터를 실행시키지 않을 경로로 지정 -->
			<exclude-mapping path="/member/login.do"/>
			<exclude-mapping path="/member/add.do"/>
			<!-- 어떤 인터셉터를 실행할지 지정 -->
			<beans:bean class="com.exam.exSpring.member.LoginInterceptor" />
		</interceptor>
	</interceptors>
	
	
	<!-- 데이터베이스 연결을 관리하는 커넥션풀로서 DBCP를 등록 -->
	<!-- DB연결을 위한 정보 입력 -->
	<beans:bean id="BasicDataSource" class="org.apache.commons.dbcp2.BasicDataSource">
		<beans:property name="driverClassName" value="oracle.jdbc.OracleDriver"/>
		<beans:property name="url" value="jdbc:oracle:thin:@localhost:1521:xe"></beans:property>
		<beans:property name="username" value="web"></beans:property>
		<beans:property name="password" value="web1234"></beans:property>
	</beans:bean>
	
	<!-- MyBatis본체를 등록 (커넥션을 제공하는 객체(DBCP)와 설정파일 위치(.xml)를 설정)-->	
	<beans:bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
  		<!-- 데이터베이스 연결을 위한 커넥션풀 정보 -->
  		<beans:property name="dataSource" ref="BasicDataSource" />
  		<!-- 설정파일 위치 -->
  		<beans:property name="configLocation" value="classpath:mybatis/mybatis-config.xml" />
	</beans:bean>

	<!--classpath:== /exSpring/src/main/resources-->
	<!-- Namespaces에서 설정이 되어있지 않아서 bean태그를 그대로 사용하면 오류발생 -> beans:bean태그 사용 -->
	<!-- ref 속성은 스프링 컨테이너에 생성되어 있는 변수를 불러오는 속성 -->
	
	<!-- SqlSessionTemplate : SqlSession구현체로서, 스레드에 안전하고, 스프링트랜잭션에 따라서 커밋,롤백을 수행하고,
							  마이바티스 예외를 스프링 예외로 변환 -->
	<!-- <beans:bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate"> -->
		<!-- 생성자의 첫번째 인자로 sqlSessionFactory라는 이름으로 스프링에 등록된 객체를 전달 -->
		<!-- <beans:constructor-arg index="0" ref="sqlSessionFactory" />
	</beans:bean> -->
	
<!-- com.exam.exSpring.member패키지 아래에 존재하는 인터페이스 중 마이바티스를 실행하는 인터페이스의 구현제를 자동생성하여 스프링에 등록  -->
<!-- annotation="org.apache.ibatis.annotations.Mapper"를 추가해서 해당 어노테이션이 붙은 인터페이스의 구현체를 생성함 -->
	<mybatis-spring:scan base-package="com.exam.exSpring.member" annotation="org.apache.ibatis.annotations.Mapper"/>
	<mybatis-spring:scan base-package="com.exam.exSpring.bbs" annotation="org.apache.ibatis.annotations.Mapper"/>
	<mybatis-spring:scan base-package="com.exam.exSpring.reply" annotation="org.apache.ibatis.annotations.Mapper"/>
	<!-- <mybatis-spring:scan base-package="com.exam.exSpring" annotation="org.apache.ibatis.annotations.Mapper"/> 
		 이렇게 폴더 전체를 잡아줘도 무방함.
	-->
	
</beans:beans>
